pre-commit:
  parallel: true
  commands:
    biome:
      glob: "**/*.{ts,tsx,js,jsx,cts,mts,cjs,mjs,json,jsonc,md,mdx}"
      run: bun run lint:staged -- {files}

pre-push:
  commands:
    biome-check:
      run: bun run lint
