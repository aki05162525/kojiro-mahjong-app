pre-commit:
  parallel: true
  commands:
    biome:
      glob: "**/*.{ts,tsx,js,jsx,cts,mts,cjs,mjs}"
      run: bun run lint:fix {staged_files}

pre-push:
  commands:
    biome-check:
      run: bun run lint
